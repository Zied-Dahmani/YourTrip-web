{{ form_start(form,{attr:{novalidate:'novalidate'}}) }}

    {{ form_label(form.nom) }}
<div class="col-sm-10">

    {{ form_widget(form.nom) }}
</div>
    <div style="color:red">{{ form_errors(form.nom) }} </div>


    <div class="col-sm-10"style="margin-top:20px">
    {{ form_label(form.description) }}
    </div>
    {{ form_widget(form.description) }}
    <div style="color:red">{{ form_errors(form.description) }} </div>

    <div class="col-sm-10"style="margin-top:20px">
        {{ form_label(form.prix) }}
    </div>
    {{ form_widget(form.prix) }}
    <div style="color:red">{{ form_errors(form.prix) }} </div>

    <div class="col-sm-10"style="margin-top:20px">
    {{ form_label(form.CentreCamping) }}
    </div>
    {{ form_widget(form.CentreCamping) }}
    <div style="color:red">{{ form_errors(form.CentreCamping) }} </div>


    <button class="btn" style="margin-top:20px">{{ button_label|default('Ajouter') }}</button>
{{ form_end(form) }}
